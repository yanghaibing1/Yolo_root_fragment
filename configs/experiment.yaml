epochs: 200
imgsz: 640
batch: 16
optimizer: AdamW
lr0: 0.008
lrf: 0.05
cos_lr: true
warmup_epochs: 5
patience: 100
seed: 42
deterministic: true
device: "0"
workers: 8
preprocessing:
  d: 7
  sigmaColor: 60
  sigmaSpace: 60
  alpha: 0.40
evaluation:
  lesion_matching_iou: 0.5
  note: NMS IoU is an inference setting and is not lesion matching IoU.
